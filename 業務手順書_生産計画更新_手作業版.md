# 生産計画更新業務 手順書(手作業版)

**この手順書は、生産計画マクロ(生産計画_マクロ.xlsm)が行っている処理をすべて人の手で再現できるように言語化したものです。**
マクロが使えない場合や、マクロの結果が正しいか確かめたい場合に使ってください。
各手順の見出しに `[StepXX]` と書いてあるのがマクロの対応ステップ番号です。

- 想定読者: Excelの基本操作(フィルタ・並べ替え・セルの数式)ができる人。業務知識は不要
- 所要時間の目安: 手作業で半日〜1日(マクロなら数分)
- 作成日: 2026-06-12(マクロ commit 4f7ea63 時点の仕様を言語化)

---

## 1. 業務の目的と全体像

オムロン様から毎月届く **BHプラン(出荷計画)** をもとに、光真製作所側の生産管理ファイル一式を最新化する業務です。
大きく3つの工程に分かれます。

```
【入力】オムロンのBHプラン(xlsx)
   │
   ▼
工程1: 日程表のデータ加工          ←マクロの「メイン実行」(Step05〜12)
   ・済んだ行を消し、計画生産を1台1行にばらし、番号を振る
   │
   ▼
工程2: 集計表・出荷完了計画の更新   ←マクロの「Phase 1-B1」(Step13〜15)
   ・日別/型式別の台数を集計シートに転記
   ・出荷・完了計画に日付行と台数を入れる
   │
   ▼
(手作業) F列の平準化              ←ここは元々マクロ対象外。人の判断
   │
   ▼
工程3: マスター更新・KMP関連       ←マクロの「Phase 1-B2」(Step17〜26)
   ・星取日程マスター、星取表、グラフ、KMP各シートを更新
```

---

## 2. 登場するファイル

すべて「実行用フォルダ」(共有の編集用フォルダ)にあります。

| ファイル | 通称 | 役割 |
|---|---|---|
| BHプラン\○○BHPlan.xlsx | **日程表** | オムロンから毎月届く出荷計画。この業務の入力。シート「日程表」を加工する |
| Ｖ８－ＢＨ計画保存版(2025年度版).xlsx | **V8保存版** | 出荷が済んだV8機の記録(台帳)。消してよい行の判定に使う |
| Ｖ９－ＢＨ計画保存版(2025年度版).xlsx | **V9保存版** | 同上のV9版 |
| Ｖ８－ＢＨ Production schedule.xlsx | **V8計画書** | V8の管理本体。「○○集計」「Ｖ８星取表」「Ｖ８出荷・完了計画」「KMP関連」シートを含む |
| Ｖ９－ＢＨ Production schedule.xlsx | **V9計画書** | V9の管理本体。「Ｖ９－星取表」「Ｖ９－BH出荷・完了計画」シートを含む |
| 星取表計算ﾏｽﾀｰ.xlsx | **計算マスター** | 「Ｖ８星取日程マスター」「V9星取表日程マスター」「光真稼働日早見表」を含む |

**作業前に必ずフォルダごとコピーしてバックアップを取ること。**

## 3. 日程表の主な列(これだけ覚えれば読める)

| 列 | 名前 | 中身 |
|---|---|---|
| B | 生産計画No | 注文の番号。例 `000389419`。1台ずつにばらすと `000389419-01` のように枝番が付く |
| C | 客先名 | 納め先 |
| F | 機種名 | `V9-BH 計画生産` のように「計画生産」と入っていたら見込み生産のかたまり行 |
| G | 型式 | |
| K | 追加仕様 | 「計画生産対象」と入っていることがある(→消す対象) |
| L | 数量 | 計画生産行は2以上のことがある。それ以外は原則1 |
| M | 順序指示発行日 | |
| N | **光真ss出荷日** | この業務で一番よく見る日付 |
| R | KP-No | 確定した機械の番号。**V9は空のことが多い** |
| S | BH型式TYPE | 集計のキー。例 `YB-LA001`(V9)、`VU-LD091`(V8) |
| U | MODEL | `V8` / `V9` / `ﾒﾝﾃV8` / `ﾒﾝﾃV9` |
| V | shipment month | Forecast集計のキー |
| W | T-No | 星取表の通し番号 |
| Y / Z | V8-LAZ型式 / V8-LAY型式 | V8の集計グループ。例 `LAZ201` / `LAY001` |
| AA〜AG | 号機 | |
| BP以降 | コンポーネント | Forecast用の部品数量 |

以下、「**当月**」= 作業する月の1日(例: 6月に作業するなら2026/6/1)とします。

---

## 4. 工程1: 日程表のデータ加工

入力のBHプランを開き、「日程表」シートに対して上から順に行います。
**順番が大事です。特に 1-2(展開)を 1-3(削除)より先にやること。**

### 1-1. 計画生産対象の行を消す `[Step05]`

- **やること**: K列(追加仕様)に「計画生産対象」と入っている行をすべて削除する
- **やり方**: K列にフィルタ→「計画生産対象」を含む→出てきた行を行ごと削除
- **なぜ**: これらは計画生産のかたまり行と二重になっている個別行のため

### 1-2. 計画生産を1台1行にばらす(展開) `[Step08]`

- **やること**: F列(機種名)に「計画生産」と入っている行のうち**数量(L列)が2以上**のものを、数量の数だけ行コピーして1台1行にする
- **やり方**(1行ずつ):
  1. その行を数量分コピーして直下に挿入(数量5なら自分含め5行に)
  2. B列の生産計画Noの末尾に `-01, -02, …` と**2桁の枝番**を付ける(例 `000389419` 数量28 → `000389419-01`〜`000389419-28`)
  3. 全行のL列(数量)を `1` にする
- **範囲**: 出荷日がいつであっても**全部**ばらす(以前は3〜4ヶ月先までに絞っていたが、現在は全展開がルール)
- **なぜ先にやるか**: 次の手順(出荷済み削除)で、星取表には枝番付きの番号で記録されているため、**枝番を付けてからでないと照合できない**

### 1-3. 出荷済みの行を消す `[Step06]`

- **やること**: もう出荷が終わっている行を日程表から消す
- **対象の条件(両方満たすもの)**:
  1. N列(光真ss出荷日)が**当月1日より前**
  2. 次のどれかの台帳にその機械が載っている
     - R列のKP-Noが **V8保存版(M列)** または **V9保存版(J列)** にある
     - KP-Noが空なら、B列の生産計画Noが **V8保存版のAZ列**・**V9保存版のAA列**・**V9計画書の「Ｖ９－星取表」AA列**(枝番付き)のどれかにある
- **やり方**: N列を昇順に並べ、当月より前の行について上の条件をCtrl+F検索やVLOOKUPで確認しながら削除
- **確認**: 終わったら、当月より前の日付の行はほぼ残っていないはず。残った行は「出荷予定日が過ぎているのにどの台帳にもない」行なので要確認

### 1-4. BH型式TYPE(S列)の穴埋め `[Step07]`

- **やること**: S列が空欄の行(メンテ行は除く)に型式を入れる
- **ルール**:
  1. G列(型式)が `YB-` か `YU-` で始まる → その値をそのままS列へ
  2. それ以外 → **同じ客先名(C列)** の別の行からS列の値をもらう(G列も同じ行があればそれを優先)
- **埋められないとき**: 出荷日が当月から3ヶ月以内なら**作業を止めてオムロン担当者に問い合わせ**。4ヶ月以上先ならメモして続行

### 1-5. 数量のチェック `[Step09]`

- **やること**: MODEL(U列)がV8/V9で出荷日が3ヶ月以内の行に、数量(L列)が1以外のものがないか確認
- **あったら**: 計画生産のばらし忘れか、データ異常。**オムロン担当者に問い合わせ**

### 1-6. 並べ替え `[Step10]`

- **やること**: データ行全体を次の優先順位で並べ替える
  1. MODEL: **V8 → ﾒﾝﾃV8 → V9 → ﾒﾝﾃV9** の順(普通の昇順ではないので注意。作業列に1/2/3/4を振ると楽)
  2. 機械品番(H列) 昇順
  3. 光真ss出荷日(N列) 昇順
  4. 順序指示発行日(M列) 昇順
  5. KP-No(R列) 昇順
  6. 属性(I列) **降順**
  7. 客先名(C列) 昇順
  8. 生産計画No(B列) 昇順

### 1-7. メンテ行の照合 `[Step11]`

- **やること**: MODELが「ﾒﾝﾃV8/ﾒﾝﾃV9」の行を、それぞれの計画書の星取表と突き合わせる
- **探し方**: まずKP-No(R列)で星取表を検索。無ければ生産計画No(B列)で検索
- **見つかったら比較**: 型式 / 出荷日 / 順序指示発行日 / 数量(星取表では同じKP-Noの行数=台数)
  - 全部一致 → OK
  - 一部だけ違う → その行を**黄色に塗って**記録し、**オムロン担当者に問い合わせ**(解決するまで先に進まない)
- **見つからなかったら**: 新規として星取表の末尾にKP-Noを追加

### 1-8. T-No(通し番号)を付ける `[Step12]`

- **やること**: V8/V9の各行のW列(T-No)を埋める
- **ルール**:
  1. 星取表(V8: KP-No=M列・生産計画No=AZ列 / V9: KP-No=J列・生産計画No=AA列)で同じ機械を探し、あればそのT-No(A列)を転記
  2. 星取表に無い新しい機械は、**星取表のT-No最大値+1** から順に新しい番号を振る
- **すでにT-Noが入っていて星取表と違うとき**:
  - 出荷日が**今月中** → 現場に影響するので**作業を止めて問い合わせ**(行を赤く塗る)
  - 来月以降 → 記録だけ残して星取表の値に直し、続行

> ここまでで日程表の加工は完了。ファイルを保存します。

---

## 5. 工程2: 集計表・出荷完了計画の更新

### 2-1. 集計シートの更新 `[Step13]`

V8計画書の「○○集計」シート(○○は日付。例 0412集計)を更新します。
シートの構造を先に理解してください:

- **上部**: 年度別・月別のまとめ(SUMIFS式)。**触らない**
- **下部の日付ブロック**: ヘッダー行+日付行(B列が日付)+最後に「総計」行。**ここだけ更新する**
- 列は「**型式別の詳細列**」(ヘッダーが `VU-LD091` や `YB-LA001` そのもの)と「**グループ列**」(ヘッダーの1行目が `LAZ201` などで下に対象型式が列挙)と「**合計・累計列**」(数式)の3種類

**手順**:
1. 日程表から、MODELがV8/V9で出荷日が**当月以降**の行だけを対象にする(過去月の集計値はいじらない)
2. 日付×型式で台数を数える(ピボットテーブルか COUNTIFS が便利)
3. 集計シートの当月以降の日付行について、**数式が入っていないセルだけ**を一旦消してから書き込む:
   - **V9の行**: S列の型式(例 YB-LA001)と同じヘッダーの列**すべて**に台数(グループ列と詳細列の両方にあることが多い)
   - **V8の行**: ①Y列のLAZ型式(例 LAZ201)とヘッダー1行目が同じグループ列、②Z列のLAY型式(例 LAY001)の列、③S列の型式(例 VU-LD091)の詳細列、の**3か所**に台数
4. **絶対のルール: 数式が入っているセルは触らない。** Ｖ８合計・Ｖ９合計・累計・ＢＨ合計の列、SUMIFSが入っている行のグループ列は自動計算されます。消すと集計が壊れます(過去に実際に起きた不具合)
5. 集計シートに無い日付が出てきたら、**日付順の正しい位置に行を挿入**する。すぐ上の行を行ごとコピーして挿入し、数値だけ消して日付を入れる(数式と書式が引き継がれる)。総計行の真上に追加すると総計のSUM範囲から外れるので、その場合は総計行のSUM範囲を最終日付行まで直す
6. 新しい型式が出てきたらヘッダーの右端に列を追加する
7. 最後に**月の区切り線を引き直す**: 日付行を上から見て、月が変わる行の下罫線を**二重線**、それ以外を細い点線にする

**検算**: 適当な1日を選び、日程表でその日の行を数えて集計シートの値と一致するか確認。

### 2-2. 出荷・完了計画に日付行を足す `[Step14]`

V8計画書の「Ｖ８出荷・完了計画」、V9計画書の「Ｖ９－BH出荷・完了計画」それぞれで:

1. **どこまで足すか**: 「当月+3ヶ月の月末」と「日程表のそのMODELの一番遅い出荷日の月末」の**遅い方**まで
2. シートの一番下にある**合計行**(B列が空でE列に `=SUM(E6:E…)` が入っている行)を見つける
3. 足りない日付について、合計行の直上に「**最後の日付行を行ごとコピー→挿入**」で1日1行追加し:
   - B列=日付、C列=曜日(月〜日)
   - D列(光真稼働)= 計算マスターの「**光真稼働日早見表**」のB列にその日付が**あれば○、なければ×**(祝日・会社休日対応)。早見表に載っていない先の日付は暫定で土日=×としておき、**祝日は手で×に直す**
   - E/F/G列などの数値は消す。**H〜O列の数式は消さずに残す**(行コピーで自動的に正しい式になる)
4. **最後に合計行のE/F/G列のSUM範囲を、追加した最終日付行まで広げる**(例 `=SUM(E6:E2685)` → `=SUM(E6:E2955)`)。これを忘れると合計から漏れる

### 2-3. 出荷台数を書き込む `[Step15]`

1. 日程表からMODEL別(V8/V9)に**日付ごとの台数合計**を数える(当月以降のみ)
2. 出荷・完了計画の各日付行の **E列(順序指示出荷台数)とG列(出荷台数LAZ計)** にその数を書く
3. 当月以降で出荷が無くなった日は、稼働日(○)なら **0** を入れ、非稼働日(×)なら空欄にする(前回の数字を残さない)
4. **F列(光真完了台数)は絶対に触らない**(次の平準化で人が入れる列)

### 2-4. (手作業)平準化

F列(光真完了台数)に、E列の出荷予定を見ながら日当たりの生産台数をならして入力します。
これは**判断を伴う作業でマクロ対象外**。従来どおり担当者が行います。

---

## 6. 工程3: マスター更新・KMP関連

### 3-1. 星取日程マスターへの転記 `[Step17][Step18]`

計算マスターの「Ｖ８星取日程マスター」「V9星取表日程マスター」に、日程表のV8/V9行(出荷日が当月以降)を転記します。

- **既にあるT-No(A列)の行**: 値を上書き更新
- **新しいT-No**: 末尾に行を追加。**先に5行目(最上データ行)のN列以降を選んでコピー→新しい行に「数式と数値の書式」貼り付け**(計算式を引き継ぐため)。その後に値を書く
- **転記の対応**: T-No(W列)→A列 / 号機(AA〜AG列)→B〜H列 / BH型式(S列)→I列 / 順序指示発行日(M列)→J列 / 光真ss出荷日(N列)→K列 / M列(13列目)に「・」

### 3-2. 星取表の差し替え `[Step19]`

V8/V9計画書の星取表を日程表の最新内容に差し替えます。

- **起点**: 原則「来月1日」以降の行。ただし日程変更(星取表とBHプランで出荷日が違う機械)があれば、その変更日まで遡る
- **差し替えてはいけない行(スキップ)**:
  - メンテ列(V8=R列、V9=N列)に「M」が入っている行
  - LAZ完了計画列(V8=N列、V9=K列)に日付が入っている行(進行中)
  - S〜AY列のどこかに日付が入っている行(工程が進んでいる)→記録を残す

### 3-3. 出荷・完了グラフ `[Step20]`

V8計画書の「BH出荷・完了グラフ」シートで、当月から4ヶ月分の月別合計(A列=`YY/MM`)を更新:
B列=V8台数、C列=V9台数、D列=合計。月の行が無ければ末尾に追加。

### 3-4. KMP関連 `[Step21][Step22][Step24][Step25][Step26]`

1. **`[Step21]` ユニット必要数の確認**: V8星取表のコンポーネント列(S〜AO列あたり)の「・」マークを日付別に数える(参考集計)
2. **`[Step22]` KMP SHIPMENT PLAN**: V8の日別出荷台数(当月以降)を、V8計画書「Ｖ８ KMP SHIPMENT PLAN」シートの該当日付行のAC列(29列目)に書く
3. **`[Step24]` KMP出荷スケジュール**: 上のAC列の値を「ＫＭＰ出荷スケジュール」シートの同じ日付行のL列・M列(12・13列目)へ転記
4. **`[Step25]` KMP MPS**: 「KMP MPS」シートの末尾に今月のブロック(V8/V9のコンポーネント別台数+TOTAL行+前回比)を追加
5. **`[Step26]` Forecast**: 日程表のshipment month(V列)をキーに、BP列以降(LAA101Q〜)のコンポーネント数量を月別に集計する

---

## 7. 作業を止めて問い合わせる場面(まとめ)

| 場面 | 連絡先 |
|---|---|
| S列の型式が埋められない(出荷3ヶ月以内) | オムロン担当者 |
| V8/V9の3ヶ月以内に数量1以外がある | オムロン担当者 |
| メンテ照合で一部だけ一致(黄色の行) | オムロン担当者 |
| **今月分**のT-Noが星取表と食い違う(赤の行) | オムロン担当者 |
| 出荷日が過去なのにどの台帳にもない行が残った | 社内で経緯確認 |

## 8. 最終チェックリスト

- [ ] 日程表: 当月より前の出荷日の行が残っていない
- [ ] 日程表: 計画生産がすべて1台1行・枝番付き(-01〜)・数量1になっている
- [ ] 日程表: 並び順が V8→ﾒﾝﾃV8→V9→ﾒﾝﾃV9
- [ ] 集計シート: 合計・累計列の数式が生きている(セルをクリックして式が見える)
- [ ] 集計シート: 総計行の値 ≒ 日程表の当月以降台数(過去月分は据え置きなので総計は一致しなくてよい。月単位でスポット確認)
- [ ] 集計シート: 月の変わり目に二重線
- [ ] 出荷・完了計画: 合計行のSUM範囲が最終日付行まで含まれている
- [ ] 出荷・完了計画: 追加行のH〜O列に式が入っている/D列の○×が祝日も正しい
- [ ] F列(完了台数)の平準化値を消していない

## 9. マクロとの対応表

| 手順書 | マクロ | 実行ボタン |
|---|---|---|
| 1-1〜1-8 | Step05→08→06→07→09→10→11→12 (この順) | メイン実行(Phase 1-A) |
| 2-1〜2-3 | Step13→14→15 | Phase 1-B1 |
| 2-4 平準化 | (マクロ対象外・手作業) | − |
| 3-1〜3-4 | Step17→18→19→20→21→22→24→25→26 | Phase 1-B2 |

> 仕様の根拠: 2026-04-21打ち合わせ指摘、2026-06ヒアリング、およびお客様提供の「４月の結果」(手作業の正解Excel)との突合により確認。
> 未確定事項(2026-06-12時点): 出荷・完了計画のE列について、3ヶ月より先を日程表の生値にするか平準化値にするか(4月の手作業では7月中旬以降が平準化値だった)。6/15打ち合わせで確認予定。
